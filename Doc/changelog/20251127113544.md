# 變更日誌

**時間：** 2025-11-27 11:35:44

**標題：** 重構為 Kawaii 雜誌風格並支援六都切換

**內容：**

重構整體 UI 為粉彩 Kawaii 風格，新增六都城市切換功能、36 小時 Hero 區塊（含城市背景圖）、三日預報 Grid 顯示（含寶可夢風格天氣圖）、雙 API 並行載入、玻璃態效果與響應式設計。

## 主要變更

### HTML (`index.html`)
- 新增 Header 區塊（日期顯示、標題）
- 新增橫向滾動城市選擇器（六個城市按鈕）
- 重構 Hero 區塊支援背景圖片
- 新增三欄 Grid 三日預報容器
- 新增 Footer 區塊

### JavaScript (`assets/js/app.js`)
- 新增 `parseDateTime()` 解決 Safari 時間格式相容性
- 新增 `transform36HourData()` 轉換 36 小時 API 資料
- 新增 `transform3DayData()` 轉換三日 API 資料並計算極值
- 新增 `renderHero()` 渲染 Hero 區塊與動態背景
- 新增 `render3DayForecast()` 渲染三日預報卡片
- 新增 `switchCity()` 城市切換邏輯
- 使用 `Promise.all()` 並行載入雙 API

### CSS (`assets/css/style.css`)
- 新增 Kawaii 粉彩色系變數（pink, mint, lavender）
- 重寫 Loading 動畫（浮動與點點動畫）
- 新增城市選擇器樣式（橫向滾動、active 狀態）
- 重寫 Hero 區塊樣式（全幅背景、漸層遮罩、玻璃態卡片）
- 新增三日預報 Grid 樣式（三欄響應式、hover 效果）
- 新增響應式斷點（480px、768px、360px）

### 文件 (`doc/plan.md`)
- 新增完整重構計畫文件

## 技術亮點

- Safari 時間格式相容性處理
- 圖片 lazy loading 優化
- 天氣圖片 fallback 機制
- 雙 API 並行載入提升效能
- 玻璃態效果（backdrop-filter）
